### Backend Bootstrap Setup

Implemented the initial backend infrastructure:

* Express application setup (`app.js`)
* Server bootstrap (`server.js`)
* MongoDB connection module (`mongodb.js`)
* Morgan request logging for development environment
* Database-first server startup flow

#### Pending Dependencies

The following modules are assumed and will be implemented by other team members:

* `config/env.js`
* `config/logger.js`
* `config/app.constants.js`
